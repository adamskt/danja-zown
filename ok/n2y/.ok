tf reconcile /clean /recursive /noprompt /exclude:.vs,.ok,*.vspscc # Updates local items on disk to match the server's structure.
Push-Location .\tools\PowerShell\; .\start_websites.ps1; Pop-Location # Starts the site, but from the root
tf get /noprompt; $(Get-Date) # Gets the latest code, and shows a timestamp so you see how stale it is later
dotnet build ./n2y.all.sln -v:m --no-incremental # Rebuilds everything, using multi-threading
dotnet build ./n2y.all.sln -v:m # Incrementally builds everything, using multi-threading
Push-Location .\tools\PowerShell\; .\_build_typescript.ps1; Pop-Location # Builds ALL the TypeScript
Push-Location .\tools\PowerShell\; .\_restore.ps1; Pop-Location # Runs Nuget restore
Push-Location .\tools\PowerShell\; .\_clean_typescript.ps1; .\_generate_typescript.ps1; .\_build_typescript.ps1; Pop-Location # Freshens all generated TypeScript
$loc = $(if ($null -eq $arg) { "" } else { Resolve-Path -Path $arg[0] }); Push-Location .\tools\PowerShell\; .\_generate_model.ps1 $loc; Pop-Location; # Generate a model for $modelDirectory
$loc = $(if ($null -eq $arg) { "" } else { Resolve-Path -Path $arg[0] }); Push-Location .\tools\PowerShell\; .\_generate_databasescripts.ps1 $loc; Pop-Location; # Generate a SQL script for $filename
Push-Location .\tools\PowerShell\; .\_watch_typescript.ps1; Pop-Location # Run TSC watchers on all scripts projects
Push-Location .\tools\PowerShell\; .\_generate_typescript.ps1; Pop-Location; # Generate TypeScript from entities
Push-Location .\tools\PowerShell\; .\_setup_urls.ps1; Pop-Location; # Setup IIS URLs
Push-Location .\tools\CodeGenerators\WebCompiler\; .\WebCompiler.bat; Pop-Location; # Recompile Sass
Get-Process -name dotnet,iisexpress -ErrorAction SilentlyContinue | Stop-Process; # Stop all sites
Get-Process -name dotnet,iisexpress -ErrorAction SilentlyContinue | Stop-Process; Push-Location .\tools\PowerShell\; .\start_websites.ps1; Pop-Location; # Restart all sites